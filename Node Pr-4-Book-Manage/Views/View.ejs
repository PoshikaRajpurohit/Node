<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= book.title %> | Book Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0; padding: 0; box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: #f7fafc;
      color: #1a202c;
    }

    .main-header {
      background: linear-gradient(to right, #2c5282, #2a4365);
      padding: 20px 0;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    .main-header .container {
      max-width: 1200px;
      margin: auto;
      padding: 0 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-family: 'Playfair Display', serif;
      font-size: 26px;
      color: #fff;
      text-decoration: none;
    }
    .nav-links a {
      text-decoration: none;
      color: #e2e8f0;
      font-weight: 600;
      margin-left: 20px;
      font-size: 16px;
    }

    .nav-links a:hover {
      color: #fff;
    }
    .back-btn {
      display: inline-block;
      margin: 30px 0 20px 20px;
      padding: 10px 20px;
      background: linear-gradient(to right, #2b6cb0, #2c5282);
      color: #fff;
      border: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 14px;
      text-decoration: none;
      transition: background 0.3s ease, transform 0.2s ease;
      box-shadow: 0 4px 12px rgba(50, 115, 220, 0.2);
    }

    .back-btn:hover {
      background: linear-gradient(to right, #2a4365, #1a365d);
      transform: translateY(-2px);
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 20px;
    }

    .view-wrapper {
      display: flex;
      gap: 40px;
      flex-wrap: wrap;
      background-color: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.06);
    }

    .book-cover {
      flex: 1;
      min-width: 280px;
      max-width: 400px;
      max-height: 480px;
      object-fit: contain;
      border: 1px solid #e2e8f0;
      padding: 12px;
      border-radius: 12px;
      background: #fefefe;
    }

    .details {
      flex: 2;
    }

    .details h2 {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      margin-bottom: 20px;
      color: #2a4365;
    }

    .detail-row {
      margin-bottom: 12px;
    }

    .detail-label {
      font-weight: 600;
      color: #2a4365;
    }

    .detail-value {
      color: #2d3748;
    }

    .description {
      margin-top: 20px;
      line-height: 1.6;
      color: #4a5568;
    }

    @media (max-width: 768px) {
      .view-wrapper {
        flex-direction: column;
      }

      .book-cover {
        max-width: 100%;
        margin: auto;
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
 <header class="main-header">
    <div class="container">
      <a href="/" class="logo">📖 Book Store</a>
      <div class="nav-links">
        <a href="/">Home</a>
        <a href="/add">Add Book</a>
      </div>
    </div>
  </header>
  <!-- BACK BUTTON -->
  <a href="/" class="back-btn">&larr; Back to Home</a>

  <!-- VIEW SECTION -->
  <div class="container">
    <div class="view-wrapper">
      <img src="<%= book.image %>" alt="Book Cover" class="book-cover"/>

      <div class="details">
        <h2><%= book.title %></h2>

        <div class="detail-row"><span class="detail-label">Author:</span> <span class="detail-value"><%= book.author %></span></div>
        <div class="detail-row"><span class="detail-label">Language:</span> <span class="detail-value"><%= book.language %></span></div>
        <div class="detail-row"><span class="detail-label">Genre:</span> <span class="detail-value"><%= book.genre %></span></div>
        <div class="detail-row"><span class="detail-label">Edition:</span> <span class="detail-value"><%= book.edition %></span></div>
        <div class="detail-row"><span class="detail-label">Publish Year:</span> <span class="detail-value"><%= book.publishedYear %></span></div>
        <div class="detail-row"><span class="detail-label">Price:</span> <span class="detail-value">₹<%= book.price %></span></div>

        <div class="description">
          <span class="detail-label">Description:</span>
          <p><%= book.description %></p>
        </div>
      </div>
    </div>
  </div>

</body>
</html>

